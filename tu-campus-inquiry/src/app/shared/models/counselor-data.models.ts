// Interface representing the data structure for a counselor
export interface CounselorData {
  // All fields are optional
  name?: string;
  jobTitle?: string;
  email?: string;
  phone?: string;
  pronouns?: string;
  details?: string;
  link?: string;
}
